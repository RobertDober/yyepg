defmodule YYepg.Parser.EmptyTest do
  # use ExUnit.Case
  
  # import YYepg.Parser, only: [parse: 1]
  # import Support.Result

  # test "empty" do
  #   assert parse("") == ok_result([])
  # end
  # test "still empty" do 
  #   assert parse("  ") == ok_result([])
  # end
  # test "and yet, still empty" do
  #   assert parse("  \n ") == ok_result([])
  # end
  # test "empty, of course" do
  #   assert parse("  \n \n ") == ok_result([])
  # end
    
end
